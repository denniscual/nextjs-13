"use client";

export default function LayoutClient() {
  return (
    <div>
      <button onClick={() => console.log("this is layout client.")}>
        This is layout client
      </button>
    </div>
  );
}
