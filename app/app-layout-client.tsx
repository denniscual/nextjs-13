"use client";

export default function AppLayoutClient() {
  return (
    <div>
      <button onClick={() => console.log(" This is app layout client")}>
        App layout client
      </button>
    </div>
  );
}
